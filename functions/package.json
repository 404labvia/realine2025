{
  "name": "functions",
  "description": "Cloud Functions REALINE — Google Calendar via Service Account",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^144.0.0"
  }
}
